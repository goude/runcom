# Not usable in git bash / cygwin
# Symptoms: weird control characters in prompt, inability
# to log in and whatnot.
# Set these manually instead
# Padding has to be entered in a text editor, since there
# is no GUI option to control it

BoldAsFont=-1
Locale=en_US
Charset=UTF-8
Font=Fira Code
FontHeight=12
Term=xterm-256color
Scrollbar=none
Padding=-2
